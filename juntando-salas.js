// Desafio: Juntar ambas as salas em uma lista só

const salaJS = ["Evaldo", "Camis", "Maria"];
const salaPython = ["Ju", "Leo", "Raquel"];

// Cria nova const unificando as duas salas
const salasUnificadas = salaJS.concat(salaPython);

console.log(salasUnificadas);
